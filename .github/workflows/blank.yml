# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "init_process"
  init_process:
    environment: 
      name: Environment_sample
   
    name: First
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    outputs:
      output1: ${{ steps.oneline.outputs.test }}
      output2: ${{ steps.anotherline.outputs.test }}
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - id: checkout
        uses: actions/checkout@v2
      - id: oneline
        run: echo "::set-output name=test::hello"
      - id: anotherline
        run: echo "::set-output name=test::world"
         
  build_code:
    environment: 
      name: Environment_sample
    
    name: Second
    runs-on: ubuntu-latest
    needs: init_process
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
       - run: echo ${{needs.init_process.outputs.output1}} ${{needs.init_process.outputs.output2}}
    
  
